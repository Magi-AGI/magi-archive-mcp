# Production environment configuration for integration tests
# Copy this file to .env.production and fill in the actual values

# Production server URL
DECKO_API_BASE_URL=https://wiki.magi-agi.org/api/mcp

# Card-based API key for integration tests (admin role)
# Generate this using the server's admin/api_keys endpoint
MCP_API_KEY=your_production_api_key_here

# Role for integration tests (user, gm, or admin)
MCP_ROLE=admin

# JWT settings (use production defaults)
JWT_ISSUER=magi-archive
JWKS_CACHE_TTL=3600
